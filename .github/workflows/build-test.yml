name: Check

on:
  pull_request: # Check Pull Requests

  push:
    branches:
      - main # Check branch after merge

concurrency:
  # Only run once for latest commit per ref and cancel other (previous) runs.
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read # for checkout

jobs:
  build:
    name: Build the sbt plugin
    uses: playframework/.github/.github/workflows/cmd.yml@v3
    with:
      java: 11
      cmd: |
        sbt Sbt-Plugin/compile

  scripted-tests:
    name: Scripted Tests
    needs:
      - "build"
    uses: playframework/.github/.github/workflows/cmd.yml@v3
    with:
      java: 11
      cmd: |
        # publishLocal everything the scripted tests resolve (sbt-scripted-tools -> sbt-plugin -> routes-compiler, play-exceptions),
        # then run the scripted test. scala.version / scala.crossversions / project.version are provided
        # by playScriptedSettings (see project/BuildSettings.scala), so no extra -D wiring is needed here.
        sbt \
          Play-Exceptions/publishLocal \
          Sbt-Routes-Compiler/publishLocal \
          Sbt-Plugin/publishLocal \
          Sbt-Scripted-Tools/publishLocal \
          "Sbt-Plugin/scripted play-sbt-plugin/show-versions"

  finish:
    name: Finish
    if: github.event_name == 'pull_request'
    needs: # Should be last
      - "build"
      - "scripted-tests"
    uses: playframework/.github/.github/workflows/rtm.yml@v3
